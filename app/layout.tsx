import type { Metadata } from "next";
import { Geist } from "next/font/google";
import "./globals.css";

const geist = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Montgomery County, MD Services Directory | MASC-NA",
  description:
    "Comprehensive directory of addiction recovery, mental health, crisis, housing, food, and support services in Montgomery County, Maryland — presented by the Montgomery Area Service Committee of Narcotics Anonymous.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${geist.variable} h-full`}>
      <body className="min-h-full antialiased">{children}</body>
    </html>
  );
}
